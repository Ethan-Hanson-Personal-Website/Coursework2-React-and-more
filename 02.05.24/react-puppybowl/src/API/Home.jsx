import React from "react"
import { useNavigate } from "react-router";

const Home = ({user}) => {
    const navigate = useNavigate();
    return(
        <div>
            <h1>Dashboard</h1>
        </div>
    )

}